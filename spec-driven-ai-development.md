# 스펙 기반 AI 개발 가이드

이 프로젝트는 설계 이후의 작업(backlog 작성 → 구현 계획 → 코드 생성)을 AI가 초안을 만들고
사람이 각 단계를 검토하는 방식으로 진행한다 — 기능/버그 수정을 코드부터 시작하지 않는다.
요건(prd) → 설계(design) → 작업 단위(backlog) → 구현 계획(plan) → 코드 순으로 이어지는 스펙
체계를 따르고, 각 단계는 문서 상단의 고정된 링크로 서로 연결되어 있어야 한다. 목적은 "왜
이렇게 만들었는지"를 나중에(다른 사람이든 AI든) 코드만 보고는 알 수 없을 때, 문서를 몇 단계만
거슬러 올라가면 찾을 수 있게 하는 것이고, 동시에 AI가 만든 산출물을 사람이 무엇을 기준으로
검토해야 하는지를 명확히 하는 것이다.

```
docs/internal/requirement/prd.md   요건 (섹션 번호가 앵커, 예: §10)
   ↑
docs/internal/design/*.md          설계
   ↑
.claude/backlogs/US-XX-*.md        작업 단위  ↔  .claude/plans/XX-*.md  구현 계획
```

## 진행 방식 — 사람 ↔ AI

설계까지는 사람이 담당하고, 그 이후(backlog → plan → 코드)는 AI가 초안을 만들고 사람이 각
단계마다 검토한다. 검토를 건너뛰고 다음 단계로 넘어가면 이 문서의 링크 규칙이 지켜지지 않은
채로 쌓이므로, 아래 순서를 생략하지 않는다.

1. **설계** — 사람이 `docs/internal/design/*.md`를 작성/수정한다.
2. **개발 항목 도출** — 새 아티팩트를 만들 필요 없이, 설계 문서 안의 하위요건 테이블(D-XX/R-XX,
   "설계 문서 작성/수정" 절 참고)의 각 행이 곧 도출된 항목이다. 여러 backlog가 하나의 표를
   나눠서 구현한다.
3. **backlog 요청 → 리뷰** — 항목별로 AI에게 backlog(US) 작성을 요청한다. 사람은 "backlog(US)
   리뷰 시 확인할 것" 절 기준으로 검토한다 — 특히 `> 설계:` 링크와 완료 기준 체크박스가
   실제로 검증 가능한 항목인지.
4. **plan 요청 → 리뷰** — 승인된 backlog를 바탕으로 구현 계획을 요청한다. 번호/슬러그가 backlog와
   맞는지, 또는 `통합(backlog 참고)` 처리가 적절한지 확인한다.
5. **구현 요청** — 승인된 plan(또는 backlog)을 바탕으로 코드 생성을 요청한다. 완료 후 "구현이
   끝나고 상태를 `done`으로 바꿀 때" 체크리스트를 사람이 직접 훑고 나서 상태를 `done`으로 바꾼다
   — AI가 스스로 체크했다고 보고해도 마지막 확인은 사람이 한다.

## 새 작업을 시작하기 전에

1. `.claude/backlogs/backlog.md`, `.claude/plans/plan.md`를 열어 이미 진행 중이거나 완료된
   작업이 있는지 확인한다.
2. 관련된 `docs/internal/design/*.md` 문서를 읽고, 기존 설계와 충돌하지 않는지 확인한다.
   새 기능이 기존 설계를 바꾼다면 이번 작업 범위에 설계 문서 갱신도 포함시킨다.

## backlog(US) 리뷰 시 확인할 것

"진행 방식" 3단계에서 AI가 초안을 만들면(사람이 직접 쓴 경우도 동일하게 적용), 승인 전에
아래를 확인한다.

- `.claude/backlogs/_TEMPLATE.md` 형식을 따랐는지 — 자유 형식이면 반려.
- 제목 바로 다음 줄에 관련 설계 문서 링크 `> 설계: [design.md](...)`가 있는지. 관련 설계
  문서가 아직 없다면 이번 작업 범위에서 새로 썼는지.
- "완료 기준"이 뭉뚱그린 문장이 아니라, 실제 테스트 케이스/수동 확인 항목과 하나씩 대응되게
  체크박스로 쪼개졌는지.
- 별도 구현 계획(plan) 파일이 필요한 경우 번호가 US 번호와 동일한지(`US-08` → `08-*.md`),
  슬러그도 backlog 파일명과 동일한지. backlog 본문에 이미 구현 상세가 충분하면 plan 파일을
  생략하고 `plans/plan.md`에 `통합(backlog 참고)`로 표기했는지.

## 설계 문서 작성/수정

- 제목 바로 다음 줄에 관련 요건 링크를 남긴다: `> 요건: [prd.md §N](...)`.
  prd의 특정 섹션과 대응되지 않는 순수 기술 문서(인프라 구조 설명 등)는 생략해도 된다.
- 여러 작업 단위가 하나의 설계 문서를 나눠서 구현하는 경우, 문서 안에 하위요건 테이블을 두고
  각 행에 `US` 컬럼으로 어떤 backlog가 그 부분을 구현했는지 표시한다.

## 구현이 끝나고 상태를 `done`으로 바꿀 때 — 매번 확인

별도 plan 파일이 없는 경우에도 아래 항목은 생략하지 않는다.

- [ ] backlog의 `> 설계:` 링크가 실제 설계 문서와 맞는지 확인
- [ ] 설계 문서의 하위요건 테이블 또는 변경 이력에 이번 작업 반영
- [ ] 구현 중 설계와 다르게 만든 부분이 있으면 설계 문서 본문도 함께 갱신
- [ ] 이번 작업으로 요건 자체가 바뀌었다면 `prd.md`도 함께 갱신
- [ ] `plans/plan.md`에 해당 행이 있는지 확인 (없으면 추가)
- [ ] `backlog.md` 인덱스의 상태와 backlog 파일 내부 상태를 동기화

## 그 밖의 코딩 컨벤션

import 경로, 예외 처리, 로깅, 테스트 스타일 등은 `CLAUDE.md`와 `.claude/rules/`를 참고한다.
스펙 추적성 상세 규칙(인덱스 파일 관리, 고정 링크 형식)은
`.claude/rules/conventions/00-progress-tracking.md`, `07-traceability.md`에 있다 — 이 문서는
그 규칙을 워크플로우 순서로 정리한 요약이며, AI(Claude Code)도 `CLAUDE.md`를 통해 세션 시작 시
이 문서를 그대로 참고한다.
