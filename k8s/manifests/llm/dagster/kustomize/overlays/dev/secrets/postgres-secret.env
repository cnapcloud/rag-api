# Placeholder dev credential for dagster's postgres connection. Must match
# the password set for the "dagster" user in infra/cnpg (see
# infra/cnpg/cluster/kustomize/overlays/dev/secrets/dagster-credentials.env).
# Replace before any non-local deployment.
postgresql-password=changeme
