apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: infra

generators:
  - ./helm/helm-chart.yaml

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: redis-auth
    envs:
      - ./secrets/redis.env