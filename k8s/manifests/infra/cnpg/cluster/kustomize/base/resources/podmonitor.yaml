apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cnpg-postgres
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: cnpg-postgres
  podMetricsEndpoints:
    - port: metrics
