# US-NN: <제목> — 설계

> 담당: `designer` · spec 워크플로우 2단계 · 템플릿: `.claude/templates/design.md`

**대상**: [spec.md](spec.md)

## 아키텍처 개요

- 컴포넌트 흐름: <API 라우터> → <Pipeline Step> → <Infra 모듈>
- 신규/변경 모듈: `<path>` (신규/수정)
- 관련 설계 문서: [<제목>](../../../docs/internal/design/<파일>.md) (있으면 — 없으면 이 줄 생략)

## 영향 레이어 / 파일

| 레이어 | 파일 | 변경 내용 |
|---|---|---|
| <레이어> | `<path>` | <신규/수정 — 무엇을> |

## API 계약 (해당 시)

```
<METHOD> <path>
  성공: <상태코드> <응답 예시>
  실패: <상태코드> <에러 예시>
```

## 데이터 모델 (해당 시)

<변경되는 스키마/필드와 이유>

## 에러 모델 (해당 시)

<예외 클래스>(`RAGError` 하위) → API 레이어에서 <상태코드>로 매핑.

## 로깅 (해당 시)

<로그 시점> — `logger.<level>("<메시지>", extra={...})`.

## NFR (해당 시)

<spec.md에 이미 있는 NFR만> 또는 (해당 없음)

## 리스크 & 롤백

- 리스크: <...> — 완화책: <...>
- 롤백: <커밋 revert로 충분한지, 추가 조치(마이그레이션 등)가 필요한지>

## 오픈 이슈 (선택)

- <있으면 여기, 없으면 "(없음)">

## 설계 검토 체크

- [ ] `architecture` 스킬 기준 레이어/의존성 방향 확인 완료 (역방향 참조 없음)
- [ ] spec.md의 모든 완료 기준(AC)이 이 설계로 커버됨
- [ ] 오픈 이슈 없음 (또는 있다면 이유와 함께 명시)
